[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-model-manager-backend"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "fastapi==0.115.12",
  "uvicorn==0.34.2"
]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
